<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cdut.mapper.MessageMapper">

    <sql id="whereClause">
        WHERE user_id = #{userId}
          AND is_deleted = 0
        <if test="msgType != null">
            AND msg_type = #{msgType}
        </if>
        <if test="isRead != null">
            AND is_read = #{isRead}
        </if>
    </sql>

    <select id="selectPage" resultType="com.cdut.pojo.SysMessage">
        SELECT id, user_id, msg_type, title, content, is_read, create_time, update_time, is_deleted
        FROM sys_message
        <include refid="whereClause"/>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*)
        FROM sys_message
        <include refid="whereClause"/>
    </select>

    <select id="countUnread" resultType="long">
        SELECT COUNT(*)
        FROM sys_message
        WHERE user_id = #{userId}
          AND is_read = 0
          AND is_deleted = 0
    </select>

</mapper>
